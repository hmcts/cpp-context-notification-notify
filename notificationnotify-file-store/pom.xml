<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>uk.gov.moj.cpp.notification.notify</groupId>
        <artifactId>notificationnotify</artifactId>
        <version>17.104.47-SNAPSHOT</version>
    </parent>

    <artifactId>notificationnotify-file-store</artifactId>
    <packaging>pom</packaging>

    <modules>
        <module>notificationnotify-file-store-bom</module>
        <module>notificationnotify-file-store-core</module>
        <module>notificationnotify-file-store-test-utils</module>
    </modules>

    <properties>
        <!-- JDK HTTP transport for Azure SDK — avoids Netty conflicts with WildFly 26's bundled Netty modules.
             Version must be compatible with the azure-core version pulled by azure-storage-blob.
             Not in common-bom. -->
        <azure-core-http-jdk-httpclient.version>1.0.0-beta.14</azure-core-http-jdk-httpclient.version>
        <!-- junit-jupiter aggregate is not in common-bom (individual sub-modules are).
             Must match common-bom's junit sub-module version (5.9.3). -->
        <junit.jupiter.version>5.9.3</junit.jupiter.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>uk.gov.moj.cpp.notification.notify</groupId>
                <artifactId>notificationnotify-file-store-core</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>uk.gov.moj.cpp.notification.notify</groupId>
                <artifactId>notificationnotify-file-store-test-utils</artifactId>
                <version>${project.version}</version>
                <scope>test</scope>
            </dependency>

            <!-- JDK HTTP transport for Azure SDK — not in common-bom -->
            <dependency>
                <groupId>com.azure</groupId>
                <artifactId>azure-core-http-jdk-httpclient</artifactId>
                <version>${azure-core-http-jdk-httpclient.version}</version>
            </dependency>

            <!-- junit-jupiter aggregate — not in common-bom; individual sub-modules are -->
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${junit.jupiter.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
